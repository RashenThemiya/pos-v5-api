info:
  name: http://localhost:8080/api/users_1
  type: http
  seq: 18

http:
  method: GET
  url: http://localhost:8080/api/users
  headers:
    - name: Authorization
      value: Bearer YOUR_TOKEN

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
