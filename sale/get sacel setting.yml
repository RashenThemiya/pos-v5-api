info:
  name: get sacel setting
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/api/scale-barcode-settings/branch/1
  headers:
    - name: Authorization
      value: Bearer YOUR_TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
