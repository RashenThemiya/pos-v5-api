info:
  name: http://localhost:8080/api/users/2/permissions
  type: http
  seq: 14

http:
  method: GET
  url: http://localhost:8080/api/users/2/permissions
  headers:
    - name: Authorization
      value: Bearer ACCESS_TOKEN

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
