info:
  name: get item by brand
  type: http
  seq: 28

http:
  method: GET
  url: http://localhost:8080/api/items/branch/1/brand/1
  headers:
    - name: Authorization
      value: Bearer YOUR_JWT_TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
