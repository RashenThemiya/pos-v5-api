info:
  name: Delete extra unit
  type: http
  seq: 53

http:
  method: DELETE
  url: http://localhost:8080/api/items/1/units/2
  headers:
    - name: Authorization
      value: Bearer YOUR_JWT_TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
