info:
  name: http://localhost:8080/api/items/2
  type: http
  seq: 46

http:
  method: DELETE
  url: http://localhost:8080/api/items/1
  headers:
    - name: Authorization
      value: Bearer YOUR_JWT_TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Delete item
