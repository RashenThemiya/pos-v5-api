info:
  name: "all permtion "
  type: http
  seq: 15

http:
  method: GET
  url: http://localhost:8080/api/permissions
  headers:
    - name: Authorization
      value: Bearer YOUR_ACCESS_TOKEN

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
