info:
  name: role permtion
  type: http
  seq: 14

http:
  method: GET
  url: http://localhost:8080/api/roles/1/permissions
  headers:
    - name: Authorization
      value: Bearer YOUR_ACCESS_TOKEN

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
