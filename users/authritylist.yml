info:
  name: authritylist
  type: http
  seq: 16

http:
  method: GET
  url: http://localhost:8080/api/authorities
  headers:
    - name: Authorization
      value: Bearer YOUR_ACCESS_TOKEN

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
