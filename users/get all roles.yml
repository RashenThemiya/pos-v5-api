info:
  name: get all roles
  type: http
  seq: 13

http:
  method: GET
  url: http://localhost:8080/api/roles
  headers:
    - name: Authorization
      value: Bearer YOUR_ACCESS_TOKEN

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
