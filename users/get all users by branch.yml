info:
  name: get all users by branch
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:8080/api/users/branch/1
  headers:
    - name: Authorization
      value: Bearer YOUR_TOKEN

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
