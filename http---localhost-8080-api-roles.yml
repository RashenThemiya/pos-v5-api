info:
  name: http://localhost:8080/api/roles
  type: http
  seq: 10

http:
  method: POST
  url: http://139.59.13.108:8080/api/roles
  headers:
    - name: Authorization
      value: Bearer ACCESS_TOKEN
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "PRODUCT_MANAGER",
        "description": "Manage products",
        "isActive": true
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
