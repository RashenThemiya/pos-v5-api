info:
  name: http://localhost:8080/api/items/1/toggle-active
  type: http
  seq: 34

http:
  method: PATCH
  url: http://localhost:8080/api/items/1/toggle-active
  headers:
    - name: Authorization
      value: Bearer YOUR_JWT_TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Change active state of item
