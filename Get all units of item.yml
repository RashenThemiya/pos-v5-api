info:
  name: Get all units of item
  type: http
  seq: 41

http:
  method: GET
  url: http://localhost:8080/api/items/1/units
  headers:
    - name: Authorization
      value: Bearer YOUR_JWT_TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
