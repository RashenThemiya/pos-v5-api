info:
  name: http://localhost:8080/api/auth/login
  type: http
  seq: 1

http:
  method: POST
  url: http://139.59.13.108:8080/api/auth/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "username":"superadmin",
        "password":"superadmin123"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
