info:
  name: http://localhost:8080/api/auth/refresh
  type: http
  seq: 5

http:
  method: POST
  url: http://139.59.13.108:8080/api/auth/refresh
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "refreshToken": "REFRESH_TOKEN_HERE"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
